services:
  telegram-stremio:
    build: .
    expose:
      - "8000"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "1m"     
        max-file: "3"      